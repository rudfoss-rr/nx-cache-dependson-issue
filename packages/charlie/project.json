{
	"name": "charlie",
	"$schema": "../../node_modules/nx/schemas/project-schema.json",
	"projectType": "library",
	"sourceRoot": "packages/charlie/src",
	"tags": [],
	"targets": {
		"build": {
			"cache": true,
			"dependsOn": ["^build"],
			"inputs": ["project"],
			"outputs": ["{projectRoot}/built/**"],
			"executor": "nx:run-commands",
			"options": {
				"command": "echo \"Build charlie @ $(date +%s)\""
			}
		}
	}
}
